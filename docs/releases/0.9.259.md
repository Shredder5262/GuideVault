# Guidevault 0.9.259

GuideVault 0.9.259 - Auth hotfix. Fixes false first-run/Create GuideVault Login screen after LaunchBox connector authentication changes when existing server users and library settings are present.
