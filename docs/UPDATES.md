# Updates

Guidevault update history is visible from the app's Info/System area.

## Docker updates

```powershell
docker compose pull
docker compose up -d
```

## Versioning

Pre-release builds currently use the `0.9.x` series.

## Release notes

See [docs/releases](releases/README.md).
